# Maya v3 ideas (out of v2 scope)

Anything noticed during the v2 build that isn't in `Maya_v2_Build_Spec.md`. Not built. Per the spec's own instruction (§ "explicitly deferred"), this goes here instead of expanding v2 scope.

---
